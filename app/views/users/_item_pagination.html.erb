<%= turbo_frame_tag 'list-pagination' do %>
  <%= paginate @collection, params: {
    controller: 'users', 
    action: @as.to_s.pluralize, 
    # format: :turbo_stream,
    user_id: @user.username
  } %>
<% end %>